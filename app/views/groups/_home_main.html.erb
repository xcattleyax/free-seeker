<div class="wrapper">
  <div class="main-sub-frame">
    グループ名<p class="group-name2"><%= @group.name %></p>
    活動内容<p class="group-content1"><%= @group.content %></p>
  </div>
  <div class="left-bar">
    <p class="group-left-top">メンバー <%= @users.length %>人</p>
    <% @users.each do |user| %>
      <p class="group-left-name"><%= user.name %></p>
    <% end %>
  </div>
</div>