<% @comments.each do |comment| %>
<div class="top-content-box"><%= link_to comment.comment, comment_path(comment.id), class:"comment-top" %>投稿タイトル: <%= comment.post.title %>
投稿者: <%= comment.user.name %>
</div>
<% end %>